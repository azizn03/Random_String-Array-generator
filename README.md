# Random_String-Array-generator
Declares three arrays which then three random integers are assigned a random string value from each array and is then pasted into a single s

UCReportGen is the main file
